# name: test/sql/metastore/test_metastore_read_bind.test
# description: Verify metastore_read bind phase
# group: [metastore]

require metastore

# We can't really execute this fully without an attached catalog right now.
# But we can verify it fails properly when missing an attached catalog.

statement error
SELECT * FROM metastore_read('missing_cat', 'db', 'table')
----
Metastore catalog missing_cat not found or unsupported
